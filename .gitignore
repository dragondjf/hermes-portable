# Build artifacts — regenerated by build_portable.sh, not committed.
hermes-portable.tar.gz
hermes-portable.tar.*
*.tar.gz

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# The built virtualenv (148M) and bundled runtime — generated at build time.
hermes-agent/venv/
runtime/

# Per-deploy state (config written by install.sh, uv binary, kanban db, etc.)
home/
home-fresh/

# Shell history / editor files
*.swp
*~
.DS_Store
